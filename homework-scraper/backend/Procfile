# Procfile for Render.com
# This is an alternative to render.yaml for simpler deployments

# Web process (Django with Gunicorn)
web: gunicorn homework_scraper.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --threads 4 --timeout 120

# Worker process (Celery)
worker: celery -A homework_scraper worker --loglevel=info --concurrency=2

# Beat process (Celery Beat for scheduled tasks)
beat: celery -A homework_scraper beat --loglevel=info --scheduler django_celery_beat.schedulers:DatabaseScheduler
